apiVersion: apps/v1
kind: Deployment
metadata:
  name: ta-hikes
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ta-hikes
  template:
    metadata:
      labels:
        app: ta-hikes
    spec:
      containers:
        - image: vicuolia/hikeservice
          name: ta-hikes
          ports:
            - containerPort: 8088
              name: ta-hikes
          env:
            - name: POSTGRES_ENDPOINT
              value: "jdbc:postgresql://localhost:5432/"
            - name: POSTGRES_PASSWORD
              value: "1qwerty2asdfg"
            - name: POSTGRES_USERNAME
              value: "travel_agency"